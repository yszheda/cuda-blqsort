// Empty TU for kernel template linkage with separate compilation
// All template implementations are in blqs_impl.hpp (header-only)
// This TU ensures device code is linked into the library
#include "blqs_impl.hpp"
